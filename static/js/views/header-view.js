const Header = {
    /**
     * Render the component content.
     */
    render: async () => {
        return `<h1 class="logo">Pluto</h1>`;
    },

    /**
     * DOM
     */
    post_render: async () => {}
};

export default Header;
