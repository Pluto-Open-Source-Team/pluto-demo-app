const Footer = {
    /**
     * Render the component content.
     */
    render: async () => {
        return `<p>Test footer</p>`;
    },

    /**
     * DOM
     */
    post_render: async () => {}
};

export default Footer;
